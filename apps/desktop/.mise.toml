[tools]

[env]
_.file = '.env'

[tasks.install]
run = "pnpm install"

[tasks.dev]
run = "pnpm dev"
depends = ["install"]

[tasks.package]
run = "pnpm package"
depends = ["install"]

[tasks.test-e2e-local]
run = "pnpm test:e2e"
depends = ["package"]
