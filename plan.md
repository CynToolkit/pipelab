- [x] **Make generated/manual Build Profiles truly normal profiles:** create all engine targets with only preferred targets enabled, use opaque `nanoid()` IDs everywhere, and ensure enabling/disabling additional targets works for auto-created profiles.
- [x] **Fix exact field validation paths:** normalize provider validation paths to indexed UI paths (`source.<field>`, `builds.N.targets.M.config.<field>`, `destinations.N.config.<field>`, `destinations.N.slots.M.config.<field>`) so errors appear beside the actual field, not only at card level.
- [x] **Wire generic producer inspection into Build Settings:** call `release:producer:inspect` for the active build, apply returned `fieldOptions`, `fieldValues`, and `issues`, and remove any accidental dependence on source inspection for producer settings such as Godot presets.
- [x] **Make “Create compatible build” actually planner-authoritative:** only offer/accept build type + engine + target candidates that the planner confirms can consume the upstream artifact and satisfy the selected destination; otherwise leave unresolved and show a clear actionable state.
- [x] **Make preference fallback future-proof:** resolve preferred user engine/targets first, then try the central deterministic fallback for the same build type if unavailable/incompatible; do not fall back to catalog order and do not change planner semantics.
- [ ] **Add regressions + verify:** cover auto-created profile target toggling, opaque IDs, exact field-path errors, producer inspection, compatible-build creation, and preference→fallback behavior; run CLI/shared/UI tests and full CI, with no new Electron feature tests.
- [x] Steam and itch account picker (at least) can't select a freshly created account connection wihtout a refresh of the interface
